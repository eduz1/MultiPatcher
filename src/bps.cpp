#include "bps.h"
#include <iostream>

// Patches any file with an .bps patch.
// std::string filePath = path to the file to be patched
// std::string bpsPath = path to the patch itself
// (optional) std::string outPath = path for the patched output file
bool PatchBPS(const std::string& filePath, const std::string& bpsPath)
{
    return true;
}